<!DOCTYPE html>
<html>
  <head>
    <title>Myapp</title>
    
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= javascript_bundle_tag 'stylesheet' %>
    <%= stylesheet_bundle_tag 'javascript' %>
    <%= stylesheet_bundle_tag 'stylesheet' %>
    <%= javascript_bundle_tag 'javascript' %>

    <%# ゆくゆくは下記にしたい %>
    <%# = javascript_bundle_tag "#{params[:controller]}#{params[:action].capitalize}"
    = stylesheet_bundle_tag "#{params[:controller]}#{params[:action].capitalize}" %>

    <%# 下記のこれあとで消す、いらなくなるかも %>
    <%# 下記は元のやつ %>
    <%# <script src="https://maps.google.com/maps/api/js?key=<%= Rails.application.credentials.api_key[:google]&language=ja&callback=initMap" async></script> %>
   <script src="https://maps.google.com/maps/api/js?key=<%= Rails.application.credentials.api_key[:google]%>&language=ja" async></script>
  </head>

  <body>
    <%= yield %> 
  </body>
</html>
